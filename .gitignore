# Secrets — never commit (spec §7)
.env
.env.local
.env.*.local

# Python
**/__pycache__/
*.py[cod]
**/.venv/
**/venv/
**/.pytest_cache/
**/.ruff_cache/
**/.mypy_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
.vite/
dist-ssr/
*.local

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Build output
apps/web/dist/

# Eval artifacts that are regenerated, not authored.
# NOTE: evals/cassettes/ and evals/baselines/ ARE committed on purpose —
# they are what makes the default PR eval cost $0.
evals/out/
في_القدس.pdf
supabase/apply_to_supabase.sql
apps/web/.env.local
